# ggplot 
    # gg stands for grammer of graphics 

install.packages("tidyverse")
library(tidyverse)

# grammer of graphics- each row is an observation and each variable is a column 
    # dplyr::filter() filter function in both stats {base} and dplyr

# mapping= aes() 

# to update website
      # knit
      # commit 
      # push
